const styles = (theme) => ({
    root: {
        flexGrow: 1,
        backgroundColor: theme.palette.background.paper,
        // color: '#000000',
    },
    theTabs: {
        position: 'absolute',
        right: '25%',
        top: 10,
    },
    pikounisLogo: {
        width: '3em',
    },
});

export default styles;
