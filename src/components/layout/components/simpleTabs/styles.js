const styles = (theme) => ({
    root: {
        position: 'absolute',
        right: 0,
        left: 0,
    },
    theTabs: {
        margin: '0 auto',
        display: 'block',
        maxWidth: 'fit-content',
    },
    pikounisLogo: {
        width: '3em',
    },
});

export default styles;
