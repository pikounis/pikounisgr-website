const styles = ( theme ) => ({
    spacer: {
        paddingBottom: 70,
    },
    title: {
        textAlign: 'center',
        paddingTop: 30,
        paddingBottom: 30,
        zIndex: 100,
        // fontSize: 50,
    },
});

export default styles;
